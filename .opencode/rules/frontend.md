# 前端规则

## 请求与错误

- 所有 API 请求必须使用 `apiFetch()`。
- 用户可见错误通过 `showHarnessError()` 呈现。
- 文档区域内的错误需要显示可读的失败原因。

## 安全

- Markdown 渲染后必须经过 DOMPurify。
- 只允许必要的标签和属性。
- data:image 输出仅用于运行代码生成的图片。

## UI 约定

- 入口保持项目卡片选择，不做营销页。
- 阅读界面优先保证文本可读性、目录可搜索、代码可运行。
- 移动端侧栏必须可收起。
